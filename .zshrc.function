function gem(){
  if [[ -x `whence -p $HOME/.rbenv/shims/gem` ]]; then
    $HOME/.rbenv/shims/gem $*
    if [ "$1" = "install" ] || [ "$1" = "i" ] || [ "$1" = "uninstall" ] || [ "$1" = "uni" ]; then
      rbenv rehash
      rehash
    fi
  else
    unfunction gem
    gem $*
  fi
}
